# Reusable Profile Settings Flow
# Usage: - runFlow: flows/profile-settings.yaml
---
# Navigate to Profile tab
- tapOn: "Perfil"
- waitForAnimationToEnd
- assertVisible: "Perfil"

# Scroll to access all settings
- scroll

# Wait for any animations
- waitForAnimationToEnd
